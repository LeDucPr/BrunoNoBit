meta {
  name: Logout
  type: http
  seq: 6
}

post {
  url: {{sso_staging}}/api/Account/Logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      
  }
}

script:post-response {
  let jsonData = res.getBody();
  if(jsonData.status == true){
  bru.setGlobalEnvVar("token", "");
  bru.setGlobalEnvVar("refreshToken", "");
  }
  
}

settings {
  encodeUrl: true
}
