meta {
  name: DealerSearch
  type: http
  seq: 4
}

post {
  url: {{cms-localhost}}/api/Dealer/DealerSearch
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
      // "DealerLevelId": "", // Guid
      // "ParentDealerId": "", //Guid 
      "Keyword": "",
      "PageSize": 10,
      "PageIndex": 0
  }
}

settings {
  encodeUrl: true
}
